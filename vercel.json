{
  "version": 2,
  "buildCommand": "npm install --legacy-peer-deps && npx prisma migrate deploy && npx prisma generate && npm run build",
  "builds": [
    { "src": "src/main.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/main.ts", "methods": ["GET", "POST", "PUT", "PATCH", "HEAD", "DELETE", "OPTIONS"]}
  ]
}
